// Skills cycled through by the typewriter effect in the Hero section.
// Edit freely — order and wording here control exactly what's typed out.
export const heroSkills: string[] = [
 "const mission = 'build the future';",
  "const expertise = ['AI', 'ML', 'Software'];",
  "const idea = await transform();",
  "function solveComplexProblems() {}",
  "const intelligence = () => innovation;",
  "async function buildIntelligentSystems() {}",
  "interface Engineer { create: boolean; }",
  "type Mindset = 'always-learning';",
  "class AIEngineer { innovate() {} }",
  "const solution = await engineer(problem);",
  "while (learning) { keepBuilding(); }",
]
